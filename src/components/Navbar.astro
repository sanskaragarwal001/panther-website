<header class="fixed top-0 left-0 w-full z-50">
  <div class="border-b border-border/40 backdrop-blur-md bg-background/30">
    <div
      class="mx-auto max-w-7xl h-16 px-2 md:px-4 lg:px-6 flex items-center justify-between"
    >
      <!-- Logo -->
      <div class="flex">
        <img src="/logo.svg" alt="Panther Logo" class="h-12 w-auto" />
      </div>

      <!-- Nav Links -->
      <nav class="hidden md:flex items-center gap-8 text-sm text-muted">
        <a href="#features" class="hover:text-text transition-colors">
          Features
        </a>
        <a href="#gallery" class="hover:text-text transition-colors">
          Gallery
        </a>
        <a href="#contact" class="hover:text-text transition-colors">
          Contact
        </a>
      </nav>

      <!-- CTA -->
      <div class="flex items-center gap-3">
        <a
          href="#contact"
          class="inline-flex items-center justify-center rounded-lg bg-primary px-4 py-2 text-sm font-medium text-black hover:bg-primaryDark transition-all duration-300"
        >
          Get Dealership
        </a>
      </div>
    </div>
  </div>
</header>
